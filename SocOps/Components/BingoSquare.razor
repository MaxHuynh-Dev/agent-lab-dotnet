@using SocOps.Models

<button
    @onclick="OnClick"
    disabled="@Square.IsFreeSpace"
    class="@GetCssClasses()"
    aria-pressed="@Square.IsMarked"
    aria-label="@(Square.IsFreeSpace ? "Free space" : Square.Text)">
    <span class="wrap-break-word hyphens-auto">@Square.Text</span>
    @if (Square.IsMarked && !Square.IsFreeSpace)
    {
        <span class="absolute top-0.5 right-0.5 text-green-600 text-xs">✓</span>
    }
</button>

@code {
    [Parameter]
    public BingoSquareData Square { get; set; } = null!;

    [Parameter]
    public bool IsWinning { get; set; }

    [Parameter]
    public EventCallback OnClick { get; set; }

    private string GetCssClasses()
    {
        var baseClasses = "relative flex items-center justify-center p-1 text-center border border-gray-200 rounded-lg transition-all duration-300 select-none min-h-[60px] text-xs leading-tight font-medium shadow-sm hover-lift";

        var stateClasses = Square.IsMarked
            ? IsWinning
                ? "bg-amber-200 border-amber-400 text-amber-900 shadow-md"
                : "bg-green-100 border-green-400 text-green-900 shadow-md"
            : "bg-white text-gray-700 hover:bg-gray-50 hover:border-gray-300";

        var freeSpaceClasses = Square.IsFreeSpace ? "font-bold text-sm" : "";

        return $"{baseClasses} {stateClasses} {freeSpaceClasses}";
    }
}
